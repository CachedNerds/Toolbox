include_rules

CFLAGS += -I ..
CFLAGS += -I conversion
CFLAGS += -I sink
CFLAGS += -I test_types
CFLAGS += -I ../sink
CFLAGS += -I ../conversion

LIBNAME += tests

: foreach *.cpp |> !cc |>
: *.o ../conversion/conversion.a conversion/conversion.a ../sink/sink.a sink/sink.a test_types/test_types.a |> !ar |> $(LIBNAME).a