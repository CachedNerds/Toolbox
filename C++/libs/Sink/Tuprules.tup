.gitignore

CC += g++

CFLAGS += -g
CFLAGS += -std=c++1z
CFLAGS += -c
CFLAGS += -I ../../

LIB += Sink
OUT_DIR += build
SOURCE_DIR += src

!cc = |> $(CC) $(CFLAGS) %f -o %o |> $(OUT_DIR)/%B.o
!ar = |> ar crs %o %f |> $(OUT_DIR)/$(LIB).a
!link = |> $(CC) $(LFLAGS) %f -o %o |>